package net.nio;

public class NIO {
    /**
     *
     * buffer channel select
     *
     * Buffer properties:
     *      1 capacity: buffer has a certain size of capacity;
     *      2 position: in written model, represent
     *      3 limit： in written model
     *
     * Channel
     *     SocketChannel read() = 0 // 无响应, -1 // 通道关闭
     *     ServerSocketChannel write() = 1;
     *
     */
}
